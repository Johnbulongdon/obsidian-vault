---
project: UntilFire
source_path: docs/design/probability-score-launch.md
migrated_from_repo: /home/adminuser/projects/UntilFire
migrated_at: 2026-05-20T09:16:16+00:00
---

---
status: ACTIVE
reviewed: true
review_rounds: 1
---
# CEO Plan: UntilFire Probability Score + Distribution Launch
Generated by /plan-ceo-review on 2026-04-25
Branch: claude/setup-gstack-locally-E87N1 | Mode: SELECTIVE EXPANSION
Repo: Johnbulongdon/UntilFire
Design doc: root-claude-setup-gstack-locally-E87N1-design-20260424-140209.md (APPROVED)

## Vision

### 10x Check
The 10x version is a FIRE co-pilot connected to real account data (Plaid integration),
with a probability score that updates daily as actual spending changes. "Your FIRE
probability dropped 2% this month — you overspent by $400 vs plan." Not a calculator:
a live financial nervous system that nudges behavior every day.

### Approach Selected
C (Parallel tracks): Build probability score + fix polish bugs + post Reddit this week.
The Reddit post can be drafted on Day 1 independent of all engineering tasks.
Feature work proceeds on Days 1-5 in the sequence below.

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | Probability score share card | S | DEFERRED | Finance is personal — validate whether users want to share before building |
| 2 | What-if savings slider on dashboard | S | ACCEPTED | Core Plan tab mechanic — reuses Monte Carlo engine, builds engagement loop |
| 3 | Birth year input in wizard (age fix) | XS | ACCEPTED | Optional field on income screen; skip = hide age from RevealScreen entirely |
| 4 | Location/tax selector for custom cities | M | ACCEPTED | Full fix: searchable STATE_TAX dropdown (US + intl entries already exist in lib) |
| 5 | Probability explanation tooltip | XS | ACCEPTED | Trust-building for first-time users on the probability score |
| 6 | Slider analytics event | XS | ACCEPTED | posthog.capture('fire_slider_adjusted') — engagement signal |

## Implementation Decisions (locked)
- D3: Slider updates probability score ONLY — base calcProjection() and chart are unaffected
- D4: Birth year is optional; if skipped, hide age from RevealScreen (show retirement year only)

## Accepted Scope (this plan)

### Core (Approach C baseline):

**Bug fixes (Day 1):**
- Fix dead `/calculator` link → `/dashboard` (`app/page.tsx:1047`)
- Fix `calcFIRE()` hardcoded year 2026 → `new Date().getFullYear()` (`lib/fire-data.ts:520`)
- Fix `calcFIRE()` hardcoded $27,400 starting balance → add `initialBalance = 0` parameter (`lib/fire-data.ts:515`)
  - Update all 6 `calcFIRE()` call sites in `app/page.tsx` (lines 883, 934, 937-940) to pass `initialBalance` (default 0)

**Analytics (Day 2):**
- Delete `instrumentation-client.ts` (bare init pattern is fragile)
- Add `<PostHogProvider apiKey={process.env.NEXT_PUBLIC_POSTHOG_KEY}>...</PostHogProvider>` wrapping `{children}` in `app/layout.tsx`
- Add `posthog.capture('wizard_step_completed', { step })` at each `onNext` in `app/page.tsx` (screens: hero, city, income, savings)
- Fire `posthog.capture('calculator_completed', { city, fireTarget, years })` in RevealScreen when `revealed` transitions to `true`
- Verify `NEXT_PUBLIC_POSTHOG_KEY` is set in Vercel project settings before deploy

**Monte Carlo probability score (Days 2-3):**
- Create `lib/monte-carlo.ts` exporting `monteCarloFIRE(inputs, runs=500)`:
  - Guard: if any required input is NaN/undefined/null → return `{ p10: null, p50: null, p90: null, probability: null }`
  - Per run: sample growthRate from uniform distribution over [0.01, 0.13] (mean 7% ± 6%)
  - Count runs where portfolio reaches `fireTarget` within 40 years
  - `probability` = count / runs (fraction of runs that reach FIRE within 40 years)
  - Return `{ p10: year, p50: year, p90: year, probability: number }`
  - 500 runs (not 1,000) to stay comfortably on main thread (~1.25ms on mobile)
  - Add TODO comment for Web Worker migration if profiling shows jank
- Hoist `calcProjection()` to parent `Page` component (`app/dashboard/page.tsx`):
  - Move both useMemo calls (lines 172 and 589) to the top-level Page component
  - Pass `{ data, fireYear, fireTarget, annualSavings }` as props to DashTab and FIRETab
  - `fireAge` already lives in parent (line 845); `retireAge = fireAge + (fireYear ?? 0)` computed in parent
- Call `monteCarloFIRE()` from parent Page component (separate useMemo, debounced 300ms for slider)
- Pass `{ p10, p50, p90, probability }` to DashTab as props
- Display on Overview tab, **above** the existing KPI grid:
  - Large probability number ("73%")
  - Subtitle "likely to retire by [p50 year]"
  - Small range: "best case: [p10] / base: [p50] / worst case: [p90]"
  - If probability = null: show "— Add your income to see your FIRE probability"

**Reddit post (Day 1, parallel — founder, not engineering):**
- Post to r/personalfinance
- Frame: "I built a free calculator that tells you when you can retire based on where you live."
- Include your own FIRE number and date. Personal = credible.
- NOT an engineering task. Engineering does not block on this.

### Cherry-picks (added to scope):

**Probability explanation tooltip (Day 3):**
- "?" icon next to the probability percentage
- Tooltip: "We simulated 500 possible futures with varying market returns. In [X]% of them, you reached your FIRE number by [p50 year]. Best case: [p10]. Worst case: [p90]."

**Birth year input (Day 3):**
- Add optional birth year field to the existing income screen (not a new wizard screen)
- Validation: clamp to [1920, currentYear - 10] — reject future years and ages under 10
- `retireAge = (currentYear - birthYear) + years` — only used for display
- If field is empty/skipped: hide the "retire at age X" line from RevealScreen (show year only)

**Location/tax selector for custom cities (Day 4):**
- When user types a city not in the 263-city database (line 195, `stateKey: "tx"`):
  - Show a searchable dropdown of ALL STATE_TAX entries from `lib/fire-data.ts`
  - Entries include US states and international countries (already exist in STATE_TAX)
  - STATE_TAX labels are already human-readable ("California — ~7.20% effective")
  - On selection: set `stateKey` from selected entry
  - Default selection: no default — user must pick. Show placeholder "Select your state or country"
  - `STATE_TAX[stateKey] ?? { rate: 0, label: 'Unknown' }` already has null safety (line 485)

**What-if savings slider (Day 4-5):**
- Location: dashboard Overview tab, below the probability score display
- Label: "If you saved $X/month instead →"
- Range: $0–min(income, $10,000)/month; step $100; default = `Math.round(annualSavings / 12 / 100) * 100`
- Slider cap: max = income (can't save more than you earn) — compute from hoisted `annualSavings` / monthly income
- On change: debounce 300ms → rerun `monteCarloFIRE()` with adjusted savings rate ONLY
  - Base `calcProjection()` and chart are NOT re-run (D3: slider updates probability score only)
- Delta display: "→ 78% likely (+5%)" in teal if higher, "→ 65% likely (-8%)" in red if lower
- Fire `posthog.capture('fire_slider_adjusted', { newMonthlySavings, probabilityDelta })` on debounce

## Implementation Sequence
1. **Day 1** — Bug fixes (dead link, year, balance + 6 call sites). Reddit post drafted by founder (parallel).
2. **Day 2** — PostHog wiring (delete instrumentation-client, add PostHogProvider, fire events). `lib/monte-carlo.ts` + hoist `calcProjection` in parent. Verify PostHog key in Vercel.
3. **Day 3** — Monte Carlo display on dashboard. Probability tooltip. Birth year field on income screen.
4. **Day 4** — Location/tax selector for custom cities. What-if slider (UI + wiring).
5. **Day 5** — Slider analytics event. QA pass (see testing strategy). Update TODOS.md (resolve items 1 + 3). Ship.

## Risk Map
| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| Monte Carlo jank on main thread | Low | 500 runs + 300ms debounce. ~1.25ms on mobile. Profile on QA pass. |
| PostHog key not set in Vercel | Medium | Verify env var before shipping Day 2 work. Silent no-op if missing. |
| Birth year field drops wizard completion | Low | Optional field, no block if skipped. If skip → hide age entirely. |
| Slider max exceeds income | Low | Cap max at income (computed from hoisted annualSavings). |
| What-if slider + Monte Carlo + hoisting shipped together | Medium | Stage as 2 PRs: (1) hoist + MC, (2) slider + cherry-picks. |
| instrumentation-client deletion breaks PostHog | Medium | Deploy PostHogProvider addition and deletion in same commit. |

## Testing Strategy
Minimum QA gate before ship:
1. Dashboard shows probability score with p10/p50/p90 range
2. Null state: no income entered → shows "—" not "NaN%"
3. Slider changes probability score only — chart does not move
4. Slider at income cap → does not exceed income
5. Custom city entry → state/country selector appears, selection changes calculated income
6. Birth year entered → RevealScreen shows correct retirement age
7. Birth year skipped → age line hidden from RevealScreen
8. `calculator_completed` fires in PostHog when user reaches RevealScreen
9. `fire_slider_adjusted` fires in PostHog on slider interaction

## Unit Tests (vitest)
- `monteCarloFIRE()` with typical inputs → probability in [0, 1]
- `monteCarloFIRE()` with NaN inputs → returns `{ probability: null }`
- `calcFIRE()` with new `initialBalance` parameter (both default 0 and explicit value)
- `calcTakeHome()` for key state keys (tx, ca, ny)

## Architecture Diagram
```
app/page.tsx (wizard)
  ├── IncomeScreen: optional birthYear field
  │     └── if birthYear: retireAge = (currentYear - birthYear) + years
  ├── CityScreen: custom city → STATE_TAX selector (US + intl)
  ├── [onNext] → posthog.capture('wizard_step_completed', { step })
  └── RevealScreen
        ├── calcFIRE() × 6 (unchanged)
        ├── revealed=true → posthog.capture('calculator_completed')
        └── if birthYear: show "retire at age X" | else: hide age line

app/dashboard/page.tsx (Page parent)
  ├── calcProjection() [single hoisted useMemo]
  │     └── returns { data, fireYear, fireTarget, annualSavings }
  ├── monteCarloFIRE() [single hoisted useMemo, lib/monte-carlo.ts]
  │     └── returns { p10, p50, p90, probability } | null on bad inputs
  ├── sliderMonthlySavings state (default: annualSavings / 12)
  ├── sliderMonteCarloResult [separate useMemo, debounced]
  ├── DashTab (Overview) ← props: projection result + MC result + slider state
  │   ├── Probability hero: "73% likely to retire by 2039" + range + tooltip
  │   ├── What-if slider: $0–income/month, updates sliderMonteCarloResult only
  │   └── [existing KPI grid — unchanged]
  └── FIRETab (Settings) ← props: projection result (no MC needed)

lib/monte-carlo.ts (NEW)
  └── monteCarloFIRE(inputs, runs=500) → { p10, p50, p90, probability } | null
```

## Deferred to TODOS.md
- Share card probability score update — validate whether finance is shareable for user cohort first
- Replace fake social proof counters with real PostHog data (needs PostHog live for 2+ weeks)
- Weekly FIRE progress email (needs returning users first)
- Web Worker for Monte Carlo (add only if profiling shows jank on target devices)
